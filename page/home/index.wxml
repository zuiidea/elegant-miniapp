<include src="../../component/common/index.wxml" />
<view class="container">
  <view class="provider-list">
    <block wx:for="{{data}}" wx:key="categoryId">
      <navigator url="/page/article/index?id={{item.categoryId}}" class="provider-item" hover-class="tranparent">
        <image src="{{item.icon}}" class="provider-icon" />
        <view class="provider-name">{{item.name}}</view>
      </navigator>
    </block>
  </view>
</view>